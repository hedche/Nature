# Per-node patch for pop (worker, 10.30.1.53)
#
# Hardware: TBD — node not yet online for discovery.
# Once online, run:
#   talosctl get disks --nodes 10.30.1.53 --insecure
# and verify the storage disk selector matches.

machine:
    network:
        hostname: pop
