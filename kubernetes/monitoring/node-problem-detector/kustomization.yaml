apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - rbac.yaml
  - configmap-kernel-monitor.yaml
  - configmap-kubelet-healthz.yaml
  - daemonset.yaml
# podmonitor.yaml moved to ../../monitoring-rules/ — it needs the PodMonitor CRD.
